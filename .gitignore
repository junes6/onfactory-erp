node_modules
dist
.DS_Store
*.local
*.log
*.tsbuildinfo
server/data/*.json
server/data/*.json.bak
server/data/documents/
server/backups/
vite.config.js
vite.config.d.ts
.wrangler/
.dev.vars*
.sites-build/
supabase/.temp/

# Claude Code local launch config (machine-specific paths)
.claude/

# 비밀은 커밋하지 않는다 — 예시 파일만 남긴다. (*.local 규칙은 .env 자체를 잡지 못한다.)
.env
.env.*
!.env.example
