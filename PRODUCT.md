# PRODUCT.md — 온팩토리 제품 기대치

이 문서는 "무엇이 맞는 상태인가"를 명문화한 기준서다. 코드·화면·데이터가 아래 기대치와 어긋나면 그것은 버그다.
매 태스크 종료 시 AGENTS.md 수칙에 따라 이 문서와 현재 구현을 대조한다.

## 1. 플랫폼 운영자는 한 계정으로 전 고객사를 관리·접속한다
- 운영자(`platform-operator`)는 로그인 화면의 워크스페이스 선택과 무관하게 로그인되어 **플랫폼 콘솔**로 진입한다.
- 콘솔의 사업체 카드 **[접속]** 으로 해당 고객사 워크스페이스 전체를 **관리자 권한**으로 사용한다(운영자 모드).
  - 세션은 운영자 신원 + 현재 진입 테넌트를 함께 유지한다. 상단 고정 배너 "운영자 모드 — {회사명} 접속 중 [나가기]".
  - 운영자 모드의 모든 행위(조회 포함)는 감사 로그에 **운영자 신원**으로 남고, 고객사 관리자는 "운영사 접속 이력"에서 이를 본다.
  - 권한 계층은 서버에서 강제한다: `requireMatchingWorkspaceIdentity`는 운영자+진입 테넌트 조합을 허용하되, 일반 계정의 교차 테넌트 접근은 차단한다.
- 승인형 "지원 세션 요청"은 보조 수단으로 남는다. 운영자 모드가 기본 접속 경로다.

## 2. 모든 표시 수치는 실데이터다 (고정 샘플 금지)
- 콘솔 사업체 지표(멤버 수·오늘 활동·미처리 티켓·마지막 활동·포인트·저장 용량)는 **스토어 집계**다. 시드·픽스처에 지표 필드를 두지 않는다.
- 어떤 화면에서도 고정 문자열을 실데이터처럼 보여주지 않는다. 데이터가 없으면 **"아직 데이터 없음"** 을 표시한다.
- 상태(정상/주의, 만료/갱신 준비 등)는 저장값이 아니라 **파생 규칙**으로 계산한다.

## 3. 워크스페이스는 코어 + 업종 모듈 구조다
- 공통 코어: AI 업무허브 · 일정 · 업무지시/결재 · 일일업무일지 · 인사/조직 · 기업 자료실 · 메신저 · 직원 성과 · 포인트(플랫폼 전용).
- 업종 모듈(`tenants.industryType`): `food_manufacturing`(제품·재고LOT·공장·판매채널·식품안전인증), `it_services`(프로젝트·산출물·계약/거래처). 확장 가능.
- 메뉴·사이드바 라벨·AI 추천 질문은 `src/modules/registry.ts` 레지스트리와 업종 enum으로만 결정한다. 화면 코드에 업종을 하드코딩하지 않는다.
- 고객사 화면에 비용·포인트 정보를 노출하지 않는다(플랫폼 콘솔 전용).

## 4. 모든 쓰기는 저장되고 재기동 후에도 유지된다
- 환경변수 없는 기본 기동은 **읽기·쓰기 JSON 저장소(원자적 저장)** 로 동작한다. 읽기 전용은 `STORE_READ_ONLY=1`을 명시했을 때만이며, 그때는 전 화면 상단에 고정 배너를 띄운다.
- 쓰기 회귀는 스토어를 직접 만드는 테스트가 아니라 **HTTP e2e(로그인→등록→재기동→유지)** 로 검증한다 (`server/default-boot.e2e.test.mjs`).
- 서버 코드를 바꾸면 서버를 재시작해야 반영된다는 사실을 항상 확인·보고한다.

## 5. 화면은 초등학생도 다음 행동을 아는 수준으로 단순하다
- 한 화면에 핵심 행동은 하나(기본 버튼 하나), 목록은 **한 줄 원칙**, 상태는 색과 쉬운 말("정상/증빙 필요/만료됨")로 보여준다.
- "지금 할 일"을 먼저 보여주고 상세는 접어 둔다. 영문 장식 라벨은 쉬운 화면 모드에서 사라진다.
- 한글 입력(IME)을 끊는 입력값 변형을 금지한다. 필수 입력은 최소화한다.
- 색·글자·간격은 `src/tokens.css` 토큰만 쓴다(검증: `scripts/verify-design-tokens.mjs`). 다크 모드는 코어 토큰 반전으로만 처리한다.

## 6. 비밀키·실데이터·고객 식별정보는 커밋하지 않는다
- `.env.local`, `server/data/*.json`, 업로드 파일, `dist/`, `.claude/`는 커밋 대상이 아니다(`.gitignore`).
- 데모 시드는 제품 코드에서 분리된 픽스처(`server/store/demo-seed.mjs`)로만 존재하고, 실고객 이름·이메일·비밀번호를 담지 않는다.
- 초기 비밀번호는 발급 직후 한 번만 보여주고 서버에는 해시만 저장한다.
