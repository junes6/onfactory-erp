# DECISIONS.md — 인더필드 확정 결정 기록

AGENTS.md가 "어떻게 일하는가", PRODUCT.md가 "무엇이 맞는 상태인가"라면 이 문서는 **"무엇을 이미 정했는가"**다.
여기 적힌 것은 다시 논의해서 뒤집기 전까지 확정이며, 새 판단이 필요할 때 근거로 삼는다.

**모든 세션은 AGENTS.md · PRODUCT.md · DECISIONS.md 세 문서를 먼저 읽고 시작한다.**

---

## 1. 제품 정체성

- **서비스명은 인더필드(InTheField)다.** 브랜드 문구는 `shared/brand.json` 한 곳에서만 정의하고 화면·서버는 `BRAND`를 참조한다. 문자열로 직접 쓰면 `pnpm verify:brand`가 막는다.
- **구조는 코어 플랫폼 + 업종 모듈이다.** 코어(AI 업무허브·일정·업무지시/결재·일지·인사·자료실·메신저·성과·세무/자산·지식재산)는 모든 고객사가 같고, 업종 모듈만 갈아 끼운다.
  - 1호 업종: **식품제조**(제품·재고LOT·공장·판매채널·식품안전인증)
  - 2호 업종: **IT서비스**(프로젝트·산출물·계약/거래처)
  - 메뉴·라벨·AI 추천 질문은 `src/modules/registry.ts`와 업종 enum으로만 결정한다. 화면 코드에 업종을 하드코딩하지 않는다.
- **북극성: 대표의 결정을 데이터로 축적해 업무유형별 자동화 레벨을 올린다.**
  제품의 성공은 화면 수가 아니라 "같은 판단을 사람이 몇 번 덜 하게 되었는가"로 잰다. 승인 큐의 결정 이력과 개인 지식 코어의 규범 카드가 그 원료다.

---

## 2. 자동화 사다리

| 레벨 | 이름 | 시스템이 하는 일 | 사람이 하는 일 |
| --- | --- | --- | --- |
| **L0** | 기록 | 무슨 일이 있었는지 남긴다 | 전부 직접 판단 |
| **L1** | 제안 | "이렇게 할까요?"를 승인 큐에 올린다 | ✓승인 / ✏수정 / ✗거절 |
| **L2** | 조건부 자동 | 정해 둔 조건 안에서는 먼저 실행하고 사후 통지한다 | 예외만 확인 |
| **L3** | 위임 | 업무유형 전체를 맡고 결과만 보고한다 | 주기적 감사 |
| **L4** | 대리 | 대표를 대신해 외부와 주고받는다 | 위임 범위 재확인 |

**승급은 업무유형별로, 대표 승인으로만 한다.** 승인률이 높다는 이유만으로 시스템이 스스로 올라가지 않는다.
**강등은 원클릭이다.** 이상하다고 느낀 순간 한 번의 클릭으로 즉시 아래 레벨로 내려오고, 내려온 이유는 기록에 남는다.

현재 구현 수준: 전 기능 **L1**이다. 승인 큐(`ai-proposals`)와 유형별 최근 4주 승인률(`automation-policies`)이 L2 승급 판단의 원료로 쌓이고 있으며, 승급 UI와 조건부 자동 실행은 아직 없다(§6 로드맵).

---

## 3. 확정 정책

1. **운영자 접속.** 플랫폼 운영자는 한 계정으로 전 고객사 워크스페이스에 관리자 권한으로 접속한다. 조회를 포함한 모든 행위는 운영자 신원으로 감사 기록에 남고, **고객사 관리자가 "운영사 접속 이력"에서 그대로 열람할 수 있다.**
2. **가입 시 3항 동의 필수.** 운영자 접속·개인정보 위탁·AI 처리 고지 세 항목에 모두 동의해야 워크스페이스가 열린다. 약관 버전이 오르면 재동의를 요구한다.
3. **스토리지 추상화.** 파일 원본은 저장소 인터페이스 뒤에 있다. **NAS가 1차**이고, 환경변수(`FILE_STORAGE_BACKEND`)만 바꾸면 클라우드로 전환된다. 라우트 코드는 바뀌지 않는다.
4. **표시 수치는 전부 실데이터.** 고정 샘플을 실데이터처럼 보여주지 않는다. 데이터가 없으면 "아직 데이터 없음"이라고 쓴다. 상태(정상/주의/만료)는 저장값이 아니라 파생 규칙으로 계산한다.
5. **AI 처리 수준 3단.** `보관만` → `정리`(분류·요약) → `활용`(제안·자동화). 고객사가 수준을 낮추면 **그 위 단계에서 만든 파생물(요약·임베딩·제안)은 파기한다.** 원문은 남는다.
6. **모델 중립.** **원문이 정본이다.** 프롬프트·렌즈·판정 기준은 코드가 아니라 DB 레코드로 두어 모델을 갈아 끼워도 축적한 지식이 그대로 남는다. 요약·임베딩은 언제든 다시 만들 수 있는 파생물로만 취급한다.
7. **가격은 미확정이다.** 모델 환산율·요금제·기본요금은 0과 "미확정"에서 시작하며, 운영자가 플랫폼 콘솔에서 확정하기 전에는 어떤 가격도 화면에 표시하지 않는다.

---

## 4. UI 원칙

- **2층 표면.** 배경 한 겹, 그 위에 카드 한 겹. **카드 안에 카드를 넣지 않는다.**
  - 결정한 값: 배경 `#F4F4F2`, 카드 흰색 + `1px #E2E2DD` 테두리 + `12px` 라운드.
  - 현재 구현값: 배경 `#F3F5F9`, 카드 경계 `#DEE1E8`, 라운드 `12px` (§6 미해결 참고).
- **토큰 변수만 쓴다.** 색·글자·간격·모서리·그림자는 `src/tokens.css`의 토큰으로만 얻는다. 화면별 hex·px는 `pnpm verify:design`이 막는다.
- **타이포는 3단 + 캡션.** 22 / 15 / 13 + 캡션 11px. **10px 이하는 쓰지 않는다.**
- **버튼은 공용 컴포넌트 하나.** `src/components/ui/Button.tsx`의 톤(primary·secondary·ghost·quiet·danger)과 크기(md·sm)만 고른다. 화면 CSS는 버튼 배치만 바꾸고 모양은 정의하지 않는다(`pnpm verify:button`).
- **목록은 한 줄 원칙.** 한 행 = 상태 점 · 제목 + 보조 1줄 · 마감 · 버튼 1개. 그 이상은 상세로 접는다.
- **상태는 쉬운 말로.** "유효/보완필요" 대신 "정상 / 증빙 필요 / 만료됨". 영문 장식 라벨은 쉬운 화면 모드에서 사라진다.
- **같은 문구·같은 버튼은 한 화면에 한 번만.** 헤더와 빈 상태에 같은 버튼을 두 번 두지 않는다.
- **판정 기준:** *"처음 보는 직원이 안내 없이 클릭 세 번 안에 시작 → 완료 보고까지 간다."* 이 문장을 통과하지 못하면 화면이 잘못된 것이다.

---

## 5. 데이터 경계

- **테넌트 격리는 불변이다.** 고객사 업무 데이터는 고객사 안에 갇힌다. 일반 계정의 교차 테넌트 접근은 서버(`requireMatchingWorkspaceIdentity`)에서 차단한다.
- **개인 지식 코어는 그 위에 얹는 별도 계층이다.** 소유자는 계정(`owner_account_id`)이며, 담기는 것은 업무 데이터가 아니라 **사람의 판단 기록**(규범 카드·노트·교정 이유·모르는 것)뿐이다. 한 계정이 여러 고객사를 오가도 자기 기록만 따라오고, 다른 계정은 API를 직접 호출해도 존재 자체를 조회할 수 없다(RLS로도 강제).
- **결재 상태머신(`업무요청 → 수행중 → 결재대기 → 결재완료`)과 3단 권한(`requireAuth` / `requireTenantAdmin` / `requirePlatformOperator`)은 불변 영역이다.** 바꾸려면 구현 전에 사유와 대안을 먼저 보고한다.

---

## 6. 남은 로드맵과 미해결 리스크

### 반드시 먼저 해결할 것

- **실제 Supabase 연결 검증이 아직이다.** 현재 Postgres 경로는 `pg-mem`으로만 검증했고 실제 인스턴스 기동 검증은 건너뛰었다(Docker 없음). **실데이터를 넣기 전에 전용 테스트 DB로 `pnpm test:pg:e2e`를 반드시 통과시킨다.** 통과 전까지 운영 저장소는 JSON이다.
- **표면 토큰이 결정값과 다르다.** §4의 배경 `#F4F4F2` / 경계 `#E2E2DD`와 현재 토큰(`#F3F5F9` / `#DEE1E8`)이 어긋나 있다. 바꿀 때는 `tokens.css`와 `scripts/verify-design-tokens.mjs`의 allowedPalette를 함께 고치고 다크 대비를 다시 잰다.

### G5 골든셋 (다음 큰 덩어리)

- **과거 결정 200건으로 새 모델을 채점한다.** 모델이나 프롬프트를 바꿀 때 "예전 판단과 얼마나 같은가"를 먼저 재고, 기준 미만이면 배포하지 않는다.
- **월 1회 AI가 10문항을 묻는다.** 답이 갈리는 지점을 스스로 찾아 대표에게 묻고, 답을 규범 카드 후보로 만든다.
- **전체 데이터 표준 형식 내보내기.** 고객사가 언제든 자기 데이터를 통째로 들고 나갈 수 있어야 한다. 잠금은 제품의 경쟁력이 아니다.

### 그 외

- 자동화 사다리 L2 이상(조건부 자동 실행·업무유형별 승급 UI·원클릭 강등)
- 외부 기회 수집 워커(저장소 밖) 실제 연결 — 인제스트 API 규격은 README에 확정
- 판매채널·NAS·메일 등 외부 서비스 실자격정보 연동

---

## 7. 작업 방식

- **절 단위로 커밋·푸시한다.** 한 절을 끝낼 때마다 타입체크·검증 게이트·테스트·빌드를 돌리고 녹색일 때만 커밋하고 즉시 푸시한다. 여러 절을 한 커밋에 섞지 않는다.
- **컨텍스트 압축 전에 반드시 커밋한다.** 압축으로 진행 중인 작업이 사라지지 않게 한다.
- **매 태스크 종료 시 대조 의무.** PRODUCT.md와 DECISIONS.md의 기대치를 현재 구현과 대조해, 지시받지 않았지만 어긋난 항목 **3개**를 보고서 말미에 제안으로 제출한다. 각 제안은 (어긋난 항목 · 현재 상태 · 고치는 방법 · 예상 규모)를 한 줄씩 적는다. 즉시 고칠 수 있는 작은 것은 고치고 "고침"으로 표시한다.
- **검증 게이트:** `pnpm verify` = 아키텍처 · 디자인 토큰 · 버튼 톤 · 브랜드. 여기에 `pnpm test:server`와 `pnpm build`까지 통과해야 완료다.
- **UI 변경은 실제 브라우저에서 끝까지 타 보고** 스크린샷 또는 DOM 확인 결과를 보고서에 남긴다. "될 것이다"는 판정이 아니다.
